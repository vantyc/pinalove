# Copy to .env for local overrides. Never commit .env.

LISTEN_ADDR=127.0.0.1:8787
BASE_PATH=/pinalove
SQLITE_PATH=./data/pinalove.sqlite
STATIC_DIR=./dist/frontend
SEED_ON_EMPTY=1
NODE_ENV=development
